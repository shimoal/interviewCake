var chai = require('chai');
var expect = chai.expect;
var largestStack = require('../20.largestStack.js');

describe('20.largestStack', function() {
  it('should be a function', function() {
    expect(largestStack).to.be.a('function');
  });
});